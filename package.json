{
  "name": "test-monorepo",
  "packageManager": "yarn@3.2.1",
  "private": true,
  "scripts": {
    "build": "yarn workspaces foreach run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^4.7.3"
  }
}
